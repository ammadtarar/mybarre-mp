<!--components/tabBar/tabBar.wxml-->
<view class="top_tab_bar">
  <view 
  style="width : {{width}}px"
  class="tab_bar {{tabIndex === index ? 'active' : 'inactive'}}" 
  wx:for="{{tabs}}" 
  wx:key="index" 
  id="{{index}}" 
  bindtap="onClick">
    {{item}}
  </view>
</view>
